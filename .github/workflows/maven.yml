name: Run Tests on addingNewGitCommands

on:
  push:
    branches:
      - addingNewGitCommands
  pull_request:
    branches:
      - addingNewGitCommands

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout code
        uses: actions/checkout@v3

      # Step 2: Set up JDK 21
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'

      # Step 3: Install Edge browser
      - name: Install Edge browser
        uses: browser-actions/setup-edge@v1
        with:
          channel: 'stable'

      # Step 4: Cache Maven dependencies
      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2-

      # Step 5: Run tests and generate allure results
      - name: Run tests
        run: mvn clean test -DexecutionType=headless -Dallure.results.directory=${{ github.workspace }}/test-outputs/allure-results

      # Step 6 (optional): Debug - List Allure results folder
      - name: List Allure results
        run: ls -lR ${{ github.workspace }}/test-outputs/allure-results

      # Step 7: Generate Allure HTML report
      - name: Generate Allure HTML report
        run: mvn allure:report -Dallure.results.directory=${{ github.workspace }}/test-outputs/allure-results

      # Step 8: Upload the Allure HTML report
      - name: Upload Allure HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: allure-html
          path: target/site/allure-maven-plugin

      # Step 9: Report build status to Jira
      - name: Report build status to Jira
        uses: atlassian/gajira-build@v2
        with:
          site: infornma-team-gk85at2g.atlassian.net
          cloud-id: 5d4e6439-9ef6-4a3d-bc1e-7d5cd73b3f9f
          email: perolo6288@infornma.com
          api-token: ATATT3xFfGF08DaNCyblhT6HqIgrQraX0sTO8XqMaBfikBbeda4uYiucfw7aTmxzSo0zkpIua49uAntXCx1wJ0eIXEfSa_TlGWMBCMLKqRA5sI2FhBvZVL7llbvK8AVe2z2XQYWsz_3wuMTX-klS3rV6MHweyVEqGASDns_I1KTuDxcRh25EIcQ=435E1930
          issue: CHATGPT-8
          status: successful
